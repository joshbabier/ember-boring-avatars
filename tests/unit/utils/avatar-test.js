import avatar from 'dummy/utils/avatar';
import { module, test } from 'qunit';

module('Unit | Utility | avatar', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = avatar();
    assert.ok(result);
  });
});
